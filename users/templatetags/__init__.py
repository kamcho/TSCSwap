# This file makes the templatetags directory a Python package, allowing Django to discover our custom template tags.
# It can be empty, but it must exist for Django to recognize the templatetags directory as a Python package.
